pipeline {
    agent any

    tools {
        // CORRECTION 1: 'Maven3' est sensible à la casse
        maven 'Maven3'
    }

    environment {
        DOCKERHUB_REPO = "rirou11/student-management"
        IMAGE_NAME = "student-management"
        IMAGE_TAG = "${env.BUILD_ID}"
    }

    stages {

        stage('Checkout SCM') {
            steps {
                echo "📥 Clonage du dépôt Git..."
                checkout scm
            }
        }

        stage('Build Maven & Package') {
            steps {
                echo "⚙️ Exécution du package Maven..."
                dir('DevOps-main') {
                    sh "mvn clean package -DskipTests"
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo "🐳 Construction de l'image Docker..."
                dir('DevOps-main') {
                    sh "docker build -t ${DOCKERHUB_REPO}:${IMAGE_TAG} ."
                    sh "docker tag ${DOCKERHUB_REPO}:${IMAGE_TAG} ${DOCKERHUB_REPO}:latest"
                }
            }
        }

        stage('Push Docker Image to Hub') {
            steps {
                echo "📤 Poussée de l'image vers Docker Hub..."
                dir('DevOps-main') {
                    // Utilisation sécurisée du credential 'docker-hub-credentials'
                    withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASSWORD')]) {
                        
                        // Connexion
                        sh "echo ${DOCKER_PASSWORD} | docker login -u ${DOCKER_USER} --password-stdin"
                        
                        // Push
                        sh "docker push ${DOCKERHUB_REPO}:${IMAGE_TAG}"
                        sh "docker push ${DOCKERHUB_REPO}:latest"
                    }
                    sh "docker logout"
                }
            }
        }
    }

    post {
        success {
            echo "✅ Succès : Pipeline terminé avec succès. Images poussées à Docker Hub."
        }
        failure {
            echo "❌ Échec : Le pipeline a échoué. Veuillez vérifier le log, notamment les identifiants Docker Hub."
        }
        // CORRECTION 2: Le bloc 'always' doit contenir une section 'steps'
        always {
            steps {
                 echo "Nettoyage en cours..."
                 // Optionnel : Nettoyer l'image locale après le push
                 // sh "docker rmi ${DOCKERHUB_REPO}:${IMAGE_TAG} || true" 
            }
        }
    }
} // <--- C'est l'accolade manquante qui a causé l'erreur 90
