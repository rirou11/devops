pipeline {
    agent any

    tools {
        // CORRECTION: Changement de 'maven3' en 'Maven3' pour correspondre à la configuration globale Jenkins
        maven 'Maven3'
    }

    environment {
        // Remplacez 'rirou11' par votre nom d'utilisateur Docker Hub si nécessaire
        DOCKERHUB_REPO = "rirou11/student-management"
        IMAGE_NAME = "student-management"
        // Le tag de l'image sera l'ID du build Jenkins
        IMAGE_TAG = "${env.BUILD_ID}"
    }

    stages {

        stage('Checkout SCM') {
            steps {
                echo "📥 Clonage du dépôt Git..."
                checkout scm
            }
        }

        stage('Build Maven & Package') {
            steps {
                echo "⚙️ Exécution du package Maven..."
                // L'exécution se fait dans le sous-dossier contenant le pom.xml
                dir('DevOps-main') {
                    sh "mvn clean package -DskipTests"
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo "🐳 Construction de l'image Docker..."
                // L'exécution se fait dans le sous-dossier contenant le Dockerfile
                dir('DevOps-main') {
                    // Utilisation de l'IMAGE_TAG pour versionner l'image
                    sh "docker build -t ${DOCKERHUB_REPO}:${IMAGE_TAG} ."
                    // Ajout du tag 'latest'
                    sh "docker tag ${DOCKERHUB_REPO}:${IMAGE_TAG} ${DOCKERHUB_REPO}:latest"
                }
            }
        }

        stage('Push Docker Image to Hub') {
            steps {
                echo "📤 Poussée de l'image vers Docker Hub..."
                // MEILLEURE PRATIQUE: Utilisation d'un credential 'Username with password'
                // NOTE: 'docker-hub-credentials' doit être de type 'Username with password'
                dir('DevOps-main') {
                    withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASSWORD')]) {
                        // Connexion
                        sh "echo ${DOCKER_PASSWORD} | docker login -u ${DOCKER_USER} --password-stdin"
                        // Push des deux tags
                        sh "docker push ${DOCKERHUB_REPO}:${IMAGE_TAG}"
                        sh "docker push ${DOCKERHUB_REPO}:latest"
                    }
                }
            }
        }
    }

    post {
        success {
            echo "✅ Succès : Pipeline terminé avec succès. Images poussées à Docker Hub."
        }
        failure {
            echo "❌ Échec : Le pipeline a échoué. Veuillez vérifier les logs ci-dessus."
        }
    }
}
